# Comparing `tmp/DDesignerAPI-0.0.1.0-py3-none-any.whl.zip` & `tmp/DDesignerAPI-0.0.1.1-py3-none-any.whl.zip`

## zipinfo {}

```diff
@@ -1,21 +1,21 @@
-Zip file size: 46537 bytes, number of entries: 19
+Zip file size: 46592 bytes, number of entries: 19
 -rw-rw-r--  2.0 unx      726 b- defN 23-Apr-24 07:15 ddesigner_api/__init__.py
 -rw-rw-r--  2.0 unx      873 b- defN 23-Apr-24 07:21 ddesigner_api/tensorflow/__init__.py
--rw-rw-r--  2.0 unx    11305 b- defN 23-Apr-24 05:44 ddesigner_api/tensorflow/dpi_blocks.py
+-rw-rw-r--  2.0 unx    11329 b- defN 23-Apr-24 10:04 ddesigner_api/tensorflow/dpi_blocks.py
 -rw-rw-r--  2.0 unx     6208 b- defN 23-Apr-24 05:44 ddesigner_api/tensorflow/dpi_layers.py
 -rw-rw-r--  2.0 unx        0 b- defN 23-Apr-24 07:24 ddesigner_api/tensorflow/examples/__init__.py
 -rw-rw-r--  2.0 unx     2328 b- defN 23-Apr-24 08:04 ddesigner_api/tensorflow/examples/examples_keras.py
 -rw-rw-r--  2.0 unx     2953 b- defN 23-Apr-24 08:04 ddesigner_api/tensorflow/examples/examples_tensorflow.py
 -rw-rw-r--  2.0 unx      790 b- defN 23-Apr-24 07:16 ddesigner_api/tensorflow/xwn/__init__.py
 -rw-rw-r--  2.0 unx    16306 b- defN 23-Apr-24 05:44 ddesigner_api/tensorflow/xwn/base_conv.py
 -rw-rw-r--  2.0 unx   141656 b- defN 23-Apr-24 05:44 ddesigner_api/tensorflow/xwn/convolutional.py
--rw-rw-r--  2.0 unx    29694 b- defN 23-Apr-24 05:44 ddesigner_api/tensorflow/xwn/keras_layers.py
+-rw-rw-r--  2.0 unx    29774 b- defN 23-Apr-24 10:03 ddesigner_api/tensorflow/xwn/keras_layers.py
 -rw-rw-r--  2.0 unx     4297 b- defN 23-Apr-24 05:44 ddesigner_api/tensorflow/xwn/keras_opt.py
--rw-rw-r--  2.0 unx     9208 b- defN 23-Apr-24 05:44 ddesigner_api/tensorflow/xwn/tf_nn.py
--rw-rw-r--  2.0 unx     3440 b- defN 23-Apr-24 05:44 ddesigner_api/tensorflow/xwn/tf_opt.py
--rwxrwxr-x  2.0 unx     9136 b- defN 23-Apr-24 09:47 DDesignerAPI-0.0.1.0.dist-info/LICENSE
--rw-rw-r--  2.0 unx     2637 b- defN 23-Apr-24 09:47 DDesignerAPI-0.0.1.0.dist-info/METADATA
--rw-rw-r--  2.0 unx       92 b- defN 23-Apr-24 09:47 DDesignerAPI-0.0.1.0.dist-info/WHEEL
--rw-rw-r--  2.0 unx       14 b- defN 23-Apr-24 09:47 DDesignerAPI-0.0.1.0.dist-info/top_level.txt
-?rw-rw-r--  2.0 unx     1793 b- defN 23-Apr-24 09:47 DDesignerAPI-0.0.1.0.dist-info/RECORD
-19 files, 243456 bytes uncompressed, 43533 bytes compressed:  82.1%
+-rw-rw-r--  2.0 unx     9224 b- defN 23-Apr-24 10:01 ddesigner_api/tensorflow/xwn/tf_nn.py
+-rw-rw-r--  2.0 unx     3456 b- defN 23-Apr-24 10:00 ddesigner_api/tensorflow/xwn/tf_opt.py
+-rwxrwxr-x  2.0 unx     9136 b- defN 23-Apr-24 10:06 DDesignerAPI-0.0.1.1.dist-info/LICENSE
+-rw-rw-r--  2.0 unx     2793 b- defN 23-Apr-24 10:06 DDesignerAPI-0.0.1.1.dist-info/METADATA
+-rw-rw-r--  2.0 unx       92 b- defN 23-Apr-24 10:06 DDesignerAPI-0.0.1.1.dist-info/WHEEL
+-rw-rw-r--  2.0 unx       14 b- defN 23-Apr-24 10:06 DDesignerAPI-0.0.1.1.dist-info/top_level.txt
+?rw-rw-r--  2.0 unx     1793 b- defN 23-Apr-24 10:06 DDesignerAPI-0.0.1.1.dist-info/RECORD
+19 files, 243748 bytes uncompressed, 43588 bytes compressed:  82.1%
```

## zipnote {}

```diff
@@ -36,23 +36,23 @@
 
 Filename: ddesigner_api/tensorflow/xwn/tf_nn.py
 Comment: 
 
 Filename: ddesigner_api/tensorflow/xwn/tf_opt.py
 Comment: 
 
-Filename: DDesignerAPI-0.0.1.0.dist-info/LICENSE
+Filename: DDesignerAPI-0.0.1.1.dist-info/LICENSE
 Comment: 
 
-Filename: DDesignerAPI-0.0.1.0.dist-info/METADATA
+Filename: DDesignerAPI-0.0.1.1.dist-info/METADATA
 Comment: 
 
-Filename: DDesignerAPI-0.0.1.0.dist-info/WHEEL
+Filename: DDesignerAPI-0.0.1.1.dist-info/WHEEL
 Comment: 
 
-Filename: DDesignerAPI-0.0.1.0.dist-info/top_level.txt
+Filename: DDesignerAPI-0.0.1.1.dist-info/top_level.txt
 Comment: 
 
-Filename: DDesignerAPI-0.0.1.0.dist-info/RECORD
+Filename: DDesignerAPI-0.0.1.1.dist-info/RECORD
 Comment: 
 
 Zip file comment:
```

## ddesigner_api/tensorflow/dpi_blocks.py

```diff
@@ -77,18 +77,18 @@
             kernel_regularizer=kernel_regularizer,
             bias_regularizer=bias_regularizer,
             activity_regularizer=activity_regularizer,
             kernel_constraint=kernel_constraint,
             bias_constraint=bias_constraint,
             dtype=dtype,
 
-            transform=True if transform is not None else False, 
+            use_transform=True if transform is not None else False, 
             bit=transform if transform is not None else 4,
             max_scale=max_scale,
-            pruning=True if pruning is not None else False,
+            use_pruning=True if pruning is not None else False,
             prun_weight=pruning if pruning is not None else 0.5,
         )
 
         self.layer_list = []
         for o in order:
             if o == 'CONV': self.layer_list.append(self.conv)
             elif o == 'BN': self.layer_list.append(self.bn)
@@ -162,18 +162,18 @@
             kernel_regularizer=kernel_regularizer,
             bias_regularizer=bias_regularizer,
             activity_regularizer=activity_regularizer,
             kernel_constraint=kernel_constraint,
             bias_constraint=bias_constraint,
             dtype=dtype,
 
-            transform=True if transform is not None else False, 
+            use_transform=True if transform is not None else False, 
             bit=transform if transform is not None else 4,
             max_scale=max_scale,
-            pruning=True if pruning is not None else False,
+            use_pruning=True if pruning is not None else False,
             prun_weight=pruning if pruning is not None else 0.5,
         )
         # self.conv = layers.Conv2D(
         #     filters, 
         #     kernel_size,
         #     strides=strides, 
         #     padding=padding, 
@@ -335,18 +335,18 @@
             kernel_regularizer=kernel_regularizer,
             bias_regularizer=bias_regularizer,
             activity_regularizer=activity_regularizer,
             kernel_constraint=kernel_constraint,
             bias_constraint=bias_constraint,
             dtype=dtype,
 
-            transform=True if transform is not None else False, 
+            use_transform=True if transform is not None else False, 
             bit=transform if transform is not None else 4,
             max_scale=max_scale,
-            pruning=True if pruning is not None else False,
+            use_pruning=True if pruning is not None else False,
             prun_weight=pruning if pruning is not None else 0.5,
         )
 
         self.layer_list = []
         for o in order:
             if o == 'TCONV': self.layer_list.append(self.tconv)
             elif o == 'BN': self.layer_list.append(self.bn)
```

## ddesigner_api/tensorflow/xwn/keras_layers.py

```diff
@@ -135,18 +135,18 @@
             bias_initializer='zeros',
             kernel_regularizer=None,
             bias_regularizer=None,
             activity_regularizer=None,
             kernel_constraint=None,
             bias_constraint=None,
             dtype='float32',
-            transform=False, 
-            pruning=False, 
+            use_transform=False, 
             bit=4, 
             max_scale=4.0,
+            use_pruning=False, 
             prun_weight=0.5,
             **kwargs
     ):
         super(Conv1D, self).__init__(
             rank=1,
             filters=filters,
             kernel_size=kernel_size,
@@ -161,16 +161,16 @@
             bias_initializer=initializers.get(bias_initializer),
             kernel_regularizer=regularizers.get(kernel_regularizer),
             bias_regularizer=regularizers.get(bias_regularizer),
             activity_regularizer=regularizers.get(activity_regularizer),
             kernel_constraint=constraints.get(kernel_constraint),
             bias_constraint=constraints.get(bias_constraint),
             dtype=dtype,
-            transform=transform, 
-            pruning=pruning, 
+            use_transform=use_transform, 
+            use_pruning=use_pruning, 
             bit=bit, 
             max_scale=max_scale,
             prun_weight=prun_weight,
             **kwargs
         )
 
 class Conv2D(Conv):
@@ -303,16 +303,16 @@
         bias_initializer="zeros",
         kernel_regularizer=None,
         bias_regularizer=None,
         activity_regularizer=None,
         kernel_constraint=None,
         bias_constraint=None,
         dtype='float32',
-        transform=False, 
-        pruning=False, 
+        use_transform=False, 
+        use_pruning=False, 
         bit=4, 
         max_scale=4.0,
         prun_weight=0.5,
         **kwargs
     ):
         super().__init__(
             rank=2,
@@ -329,16 +329,16 @@
             bias_initializer=initializers.get(bias_initializer),
             kernel_regularizer=regularizers.get(kernel_regularizer),
             bias_regularizer=regularizers.get(bias_regularizer),
             activity_regularizer=regularizers.get(activity_regularizer),
             kernel_constraint=constraints.get(kernel_constraint),
             bias_constraint=constraints.get(bias_constraint),
             dtype=dtype,
-            transform=transform, 
-            pruning=pruning, 
+            use_transform=use_transform, 
+            use_pruning=use_pruning, 
             bit=bit, 
             max_scale=max_scale,
             prun_weight=prun_weight,
             **kwargs
         )
 
 class Conv2DTranspose(Conv2D):
@@ -459,16 +459,16 @@
             bias_initializer='zeros',
             kernel_regularizer=None,
             bias_regularizer=None,
             activity_regularizer=None,
             kernel_constraint=None,
             bias_constraint=None,
             dtype='float32',
-            transform=False, 
-            pruning=False, 
+            use_transform=False, 
+            use_pruning=False, 
             bit=4, 
             max_scale=4.0,
             prun_weight=0.5,
             **kwargs
         ):
         super(Conv2DTranspose, self).__init__(
             filters=filters,
@@ -483,16 +483,16 @@
             bias_initializer=initializers.get(bias_initializer),
             kernel_regularizer=regularizers.get(kernel_regularizer),
             bias_regularizer=regularizers.get(bias_regularizer),
             activity_regularizer=regularizers.get(activity_regularizer),
             kernel_constraint=constraints.get(kernel_constraint),
             bias_constraint=constraints.get(bias_constraint),
             dtype=dtype,
-            transform=transform, 
-            pruning=pruning, 
+            use_transform=use_transform, 
+            use_pruning=use_pruning, 
             bit=bit, 
             max_scale=max_scale,
             prun_weight=prun_weight,
             **kwargs
         )
     
         self.output_padding = output_padding
@@ -503,18 +503,18 @@
             if out_pad >= stride:
               raise ValueError('Stride ' + str(self.strides) + ' must be '
                                'greater than output padding ' +
                                str(self.output_padding))
 
         # Add optimization kernel
         self.opt = Optimization(
-            use_transform=transform, 
+            use_transform=use_transform, 
             bit=bit, 
             max_scale=max_scale,
-            use_pruning=pruning, 
+            use_pruning=use_pruning, 
             prun_weight=prun_weight
         )
     
         def build(self, input_shape):
           input_shape = tensor_shape.TensorShape(input_shape)
           if len(input_shape) != 4:
             raise ValueError('Inputs should have rank 4. Received input '
```

## ddesigner_api/tensorflow/xwn/tf_nn.py

```diff
@@ -20,16 +20,16 @@
 
 def conv1d(input,  # pylint: disable=redefined-builtin
             filters,
             strides,
             padding,
             data_format="NHWC",
             dilations=None,
-            transform=False, 
-            pruning=False, 
+            use_transform=False, 
+            use_pruning=False, 
             bit=4, 
             max_scale=4.0,
             prun_weight=0.5,
             name=None):
 
     # pylint: disable=line-too-long
     r"""Computes a 1-D convolution given 3-D input and filter tensors.
@@ -182,18 +182,18 @@
       name: A name for the operation (optional).
     Returns:
       A `Tensor`. Has the same type as `input` and the same outer batch shape.
     """
     # pylint: enable=line-too-long
     filters = optimization(
         filters, 
-        use_transform=transform, 
+        use_transform=use_transform, 
         bit=bit, 
         max_scale=max_scale,
-        use_pruning=pruning, 
+        use_pruning=use_pruning, 
         prun_weight=prun_weight,
     )
     
     return tf.nn.conv2d(input,  # pylint: disable=redefined-builtin
                   filters,
                   strides,
                   padding,
```

## ddesigner_api/tensorflow/xwn/tf_opt.py

```diff
@@ -16,37 +16,14 @@
 import os
 os.environ['TF_DETERMINISTIC_OPS'] = '0'
 
 import tensorflow as tf
 
 
 
-def optimization(
-    x, 
-    use_transform=False, 
-    bit=1, 
-    max_scale=4.0,
-    use_pruning=False, 
-    prun_weight=0.50, 
-    method='L1',
-    ):
-    x_tr = transform(
-        x, 
-        bit=bit, 
-        max_scale=max_scale, 
-        valid=use_transform,
-        method=method,
-    )
-
-    x_mask = pruning(x, valid=use_pruning, weight=prun_weight)
-    x_opt = x_tr * x_mask
-
-    return x_opt
-
-
 def transform(
     x, 
     bit=1, 
     max_scale=4.0, 
     method='L1', 
     valid=False):
     '''
@@ -128,7 +105,30 @@
         tf.zeros_like(x_mag, dtype=x.dtype), 
         tf.ones_like(x_mag, dtype=x.dtype)
     ) # (i,o)
 
     return mask
 
 
+def optimization(
+    x, 
+    use_transform=False, 
+    bit=1, 
+    max_scale=4.0,
+    use_pruning=False, 
+    prun_weight=0.50, 
+    method='L1',
+    ):
+    x_tr = transform(
+        x, 
+        bit=bit, 
+        max_scale=max_scale, 
+        valid=use_transform,
+        method=method,
+    )
+
+    x_mask = pruning(x, valid=use_pruning, weight=prun_weight)
+    x = tf.compat.v1.assign(x, x_tr * x_mask)
+
+    return x
+
+
```

## Comparing `DDesignerAPI-0.0.1.0.dist-info/LICENSE` & `DDesignerAPI-0.0.1.1.dist-info/LICENSE`

 * *Files identical despite different names*

## Comparing `DDesignerAPI-0.0.1.0.dist-info/METADATA` & `DDesignerAPI-0.0.1.1.dist-info/METADATA`

 * *Files 14% similar despite different names*

```diff
@@ -1,22 +1,22 @@
 Metadata-Version: 2.1
 Name: DDesignerAPI
-Version: 0.0.1.0
+Version: 0.0.1.1
 Summary: Deep-learning Designer: Deep-Learning Training Optimization & Layers API(like Keras)
 Home-page: https://github.com/DPI/TrainingAPI
 Author: dean
 Author-email: dean@deeper-i.com
 License: Apache-2.0
 Keywords: dpi,xwn,tensorflow,keras
 Platform: UNKNOWN
 Classifier: Programming Language :: Python :: 3.7
 Requires-Python: >=3.7
 Description-Content-Type: text/markdown
 
-[DDesinger API] Deep-learning Designer API
+[DDesigner API] Deep-learning Designer API
 ==========================================
 
 # 1. About
 ## 1.1. DDesignerAPI?
 It is a API for deep-learning learning and inference, and an API for application development using multi-platform
 
 ## 1.2. Functions
@@ -58,35 +58,39 @@
         >>> from ddesigner_api.tensorflow import dpi_blocks as db
         >>> dtype='mixed_float16'
         >>> db.Conv2DBlock(
             64, (3,3), strides=(1,1), padding='SAME', use_bias=True,
             activation=tf.keras.layers.ReLU(dtype=dtype), 
             batchnormalization=tf.keras.layers.BatchNormalization(dtype=dtype), 
             dtype=dtype,
-            transform=4, pruning=0.5
+            transform=4, max_scale=4.0,
+            pruning=0.5
         )
 
 ## 3.2. XWN 
 ### 3.2.1. tensorflow
         >>> from ddesigner_api.tensorflow.xwn import tf_nn as nn
         >>> nn.conv2d(
                 x,
                 kernel,
                 ...
-                transform=True,
+                use_transform=True,
                 bit=4,
-                max_scale=4.0
+                max_scale=4.0,
+                use_pruning=False
             )
 ### 3.2.2. keras
         >>> from ddesigner_api.tensorflow.xwn import keras_layers as klayers
         >>> klayers.Conv2D(
-            2, 3, 
-            ...
-            transform=True,
-            bit=4,
-            max_scale=4.0
+                2, 3, 
+                ...
+                use_transform=True,
+                bit=4,
+                max_scale=4.0
+                use_pruning=True,
+                prun_weight=0.5
             )
 
 # 4. Next
 ## 4.1. Pytorch
```

